<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.AbsolverDatabase" parent="Base.Theme.AbsolverDatabase">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_launcher_ab_color_foreground</item>
        <item name="android:windowSplashScreenBackground">@color/high_level_black_deep</item>
    </style>
</resources>